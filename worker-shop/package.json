{
  "name": "mal-griot-book-shop-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "db:init": "wrangler d1 execute book-shop --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute book-shop --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
